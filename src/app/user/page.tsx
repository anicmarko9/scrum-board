'use client';

import UserSettings from '@/components/UserSettings/UserSettings';
import Layout from '@Layout/Layout';

const page = () => {
  return (
    <Layout>
      <UserSettings />
    </Layout>
  );
};

export default page;
