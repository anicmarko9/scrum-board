'use client';

import UserSettings from '@Components/user-settings/UserSettings';
import Layout from '@Layout/Layout';

const page = () => {
  return (
    <Layout>
      <UserSettings />
    </Layout>
  );
};

export default page;
