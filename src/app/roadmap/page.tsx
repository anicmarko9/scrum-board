'use client';

import Roadmap from '@/components/Roadmap/Roadmap';
import Layout from '@/components/layout/Layout';

const page = () => {
  return (
    <Layout>
      <Roadmap />
    </Layout>
  );
};

export default page;
